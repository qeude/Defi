# Optional. Required when multiple Apple Development identities are installed.
DEFI_DEVELOPMENT_TEAM=

# Exact identity name or SHA-1 fingerprint overrides the team selection.
# DEFI_CODESIGN_IDENTITY=
